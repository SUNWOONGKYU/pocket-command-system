{
  "_comment": "콕핏 프로젝트 매핑 예시 — 공개본 기본값. 실제 운영 데이터는 config/projects.local.json(gitignore)에 두면 그쪽이 우선 사용된다. worker/auditor는 agents.name과 정확히 일치해야 함. label은 카드 표시명. git은 경영 계층(최근 커밋) 표시용(선택).",
  "projects": [
    { "id": "project-a", "label": "Project A", "worker": "Worker 1", "auditor": "Worker 1 Auditor", "git": "" },
    { "id": "project-b", "label": "Project B", "worker": "Worker 2", "auditor": "Worker 2 Auditor", "git": "" }
  ]
}
